<h1>Listing followed_companies</h1>

<table>
  <tr>
    <th>User</th>
    <th>Company</th>
    <th>Preference</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @followed_companies.each do |followed_company| %>
  <tr>
    <td><%= followed_company.user %></td>
    <td><%= followed_company.company %></td>
    <td><%= followed_company.preference %></td>
    <td><%= link_to 'Show', followed_company %></td>
    <td><%= link_to 'Edit', edit_followed_company_path(followed_company) %></td>
    <td><%= link_to 'Destroy', followed_company, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Followed company', new_followed_company_path %>
