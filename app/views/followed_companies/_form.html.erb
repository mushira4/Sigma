<%= form_for(@followed_company) do |f| %>
  <% if @followed_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@followed_company.errors.count, "error") %> prohibited this followed_company from being saved:</h2>

      <ul>
      <% @followed_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :company %><br />
    <%= f.text_field :company %>
  </div>
  <div class="field">
    <%= f.label :preference %><br />
    <%= f.text_field :preference %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
